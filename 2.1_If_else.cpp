#include <iostream>

using namespace std;

int main()
{
	int a;
	cin >> a;
	if (a % 2 == 0)
	{
		cout << "Even" << endl;
	}
	else if cout << "Odd" << endl; // If only one command, the {} could be ignored 
	else cout << "Zero" << endl;
}
int main()
{
	int b;
	cin >> b;
	if (b < 10 && b > 0) cout "This number is between 0 & 10" << endl; // && means "and"
}
int main()
{
	int c;
	cin >> c;
	if (c < 10 || c > 0) cout "This number is between 0 & 10" << endl; // || means "or"
}
